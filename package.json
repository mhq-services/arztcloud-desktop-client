{
  "name": "arztcloud-desktop-client",
  "productName": "arztcloud Desktop Client",
  "version": "0.4.0",
  "main": "src/main.js",
  "description": "Desktop Client für arztcloud.com",
  "author": "MHQ - Services GmbH",
  "scripts": {
    "start": "node_modules/.bin/electron .",
    "publish": "rm -r dist && electron-builder build --mac --win -p always"
  },
  "devDependencies": {
    "electron": "^9.1.1",
    "electron-builder": "^22.7.0"
  },
  "dependencies": {
    "electron-context-menu": "^2.2.0",
    "electron-log": "^4.2.2",
    "electron-updater": "^4.3.1"
  }
}
